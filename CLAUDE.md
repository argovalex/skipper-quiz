# SkipperQuiz — הנחיות לסוכנים

## הוסף, אל תחליף (חוק כללי — אלכס 2026-08-23)
כשמבקשים "תוסיף X" לדף/תפריט/קובץ קיים — **מוסיפים פריט, לא דורסים את המבנה הקיים**. אל תחליף דף שלם כדי להכניס פונקציה אחת. אם התוכן החדש גדול, שים אותו בקובץ נפרד וקשר אליו מהתפריט הקיים. (רקע: `hub.html` היה תפריט Control Hub; קומיט a24f9ee דרס אותו לגמרי בדף ניהול מדריכים במקום להוסיף אליו קישור "ניהול". שוחזר, וה-admin עבר ל-`admin.html`.)

## מקור האמת לשאלות
- **`data/l11.json`** ו-**`data/l30.json`** הם המקור הקנוני (per-license). ערוך אותם בלבד לצורך עבודה על תוכן שאלות.
- **אל תקרא ואל תערוך** את הקבצים הבאים לצורך עבודה על תוכן — הם נגזרים/מיוצרים וגדולים, וקריאתם שורפת טוקני קונטקסט לחינם:
  - `questions.json` — נגזר מ-`data/`. נדרש ב-runtime (ה-publisher קורא אותו). נשאר tracked, אבל לא לעריכה ידנית.
  - `quiz-data-l11.json` — נגזר. נדרש ב-runtime (`quiz-app.html` קורא אותו). נשאר tracked, לא לעריכה ידנית.
  - `html/` — 355 קבצי HTML מיוצרים. **לא tracked ב-git** (ב-.gitignore). לעולם אל תקרא אותם ידנית.

## צירוף טקסט VO לכל סרטון (חוק קבוע — אלכס 2026-08-12)
**בכל פעם שמיוצר סרטון, חובה לצרף את טקסט ה-VO (הקריינות המלאה) יחד עם הסרטון.** כשמעבירים/שולחים סרטון למשתמש — שולחים לצדו גם את טקסט ה-VO (קובץ ה-`.txt` המלווה). זה חל על **כל** סרטון: מערך שיעור, רינדור שאלה, ריל. בלי טקסט ה-VO מצורף, המסירה לא שלמה.
- **הקובץ חייב להיות מנוקד** — הרץ את מילון הניקוד (`scratch_niqqud.apply_lexicon`) על הטקסט לפני שמירה, כך שהקומפניון מציג את ההגייה בפועל ולא כתיב חלק.
- **המילון הוא הקובע.** כתוב את מילות ה-VO בכתיב **זהה** למפתח שבמילון (`references/niqqud-lexicon.md`). ההתאמה לפי אותיות — כתיב מלא מול חסר (למשל "שישים" מול "ששים") לא ייתפס. אם נתקלים בהבדל כתיב, מוסיפים וריאנט למילון לפי הצורך; אלכס מתחזק את המילון ידנית גם כן.

## איך לרנדר תוצרים
- `html/` מיוצר ע"י `node src/generate-all-l11.js` (ו-`src/generate-compass-reel.js` לריל). הרץ אחרי שינוי תוכן; אל תערוך את `html/` ביד.
- **רינדור וידאו של שאלה: השתמש ב-`node tools/quiz-app/update-question.js <num>`.** הוא מרנדר עם ה-VO והויזואל הנכונים ומפיץ לכל המקומות (data/l11.json, quiz-data-l11.json, quiz-app.html).
- **קריטי:** רינדור דרך ה-API של השרת **חייב לשלוח את פרמטר `html`** (הויזואל מ-`scenes.js`, דרך `tools/quiz-app/scene-html.js`). `html/` לא tracked, אז השרת לא יכול למשוך אותו — בלי `html` הסרטון יוצא **גנרי** (עוגן במקום הדיאגרמה). לעולם אל תשלח `/render` רק עם `voiceover_text`.

## ארכוב מערכי שיעור (חוק קבוע — אלכס 2026-08-09)
כל מערך שיעור = **תיקייה אחת בשם הנושא (עברית)** תחת `lessons/l11/cards/<נושא>/`, שמכילה את **כל** התוצרים יחד:
- `<נושא>.mp4` — הסרטון הסופי.
- כל כרטיסי ה-PNG של השיעור.
- `<נושא>.txt` — הגרסה האחרונה של הקובץ המלווה.
- שם התיקייה הוא הנושא, **לא** `uX`. דוגמאות: `cards/אזורי שיט ומהירויות/`, `cards/דגל צולל/`.
- לשיעור חדש: צור `lessons/l11/cards/<נושא>/`, העתק לשם את הכרטיסים מ-`<scratchpad>/uX/cards/*.png`, את ה-mp4 הממותג, ואת ה-txt. עדכן את הקבצים בתיקייה הזו בכל שינוי.
- מקורות ה-scratch נמחקים/מסתובבים — הארכוב ב-`cards/<נושא>/` הוא ה**מקור המתמשך**. אל תסתמך על ה-scratchpad שיישאר.

## מיתוג סרטוני שיעור (חוק קבוע — אלכס 2026-08-11)
- הסרטון **לא** מתחיל בסרטון מונפש. **אל תשתמש ב-`media/intro-meshit11.mp4`** בתור פתיח. (המחדל השגוי הזה חזר על עצמו — הזיכרון הישן טעה.)
- פתיח וסיום = **כרטיס לוגו סטטי** מ-`media/my_logo.jpg`: "Alex Argov · Sailing Instructor · www.alargov.com". הסיום עם תגית "תודה שצפיתם". + סימן מים בפינה השמאלית-עליונה לאורך כל השיעור.
- **המיתוג הוא קוד קבוע, לא תיאור לשחזר: הרץ `python tools/lesson/brand.py <lesson.mp4> [branded.mp4]`.** הוא בונה את כרטיסי הלוגו (פתיח 3ש׳ / סיום 4ש׳ עם "תודה שצפיתם") ומטביע את סימן המים לכל האורך. אל תרכיב מיתוג ב-scratch מחדש ואל תשאל את אלכס איך — זה כבר נקבע וקומפל לכלי.
- הרפרנס המחייב הוא כל מערך קיים, למשל `cards/דגל צולל/דגל צולל.mp4` (מתחיל בכרטיס הלוגו), **לא** גרסה שמקדימה וידאו. (הכלי הוצא מ-`scratch_brand_u12.py`, שהיה נמחק עם ה-scratchpad וגרם לבקשה החוזרת.)

## סרטון אנכי חינמי לשאלה (מקומי, בלי ענן בתשלום)
- `python -m video.make_question_video <num> [--voice avri|hila] [--all] [--bg-clip <path>] [--license 11]` מפיק סרטון 9:16 לשאלה בלי שרת Railway ובלי Cloudinary. פלט ב-`output/videos/q<num>.{mp4,srt,mp3}`.
- הויזואל הוא ה**קנוני** מ-`scenes.js` (מרונדר מקומית עם puppeteer דרך `video/render_frames.js`) — לא כרטיסי Pillow חדשים. הקריינות היא `buildVoiceover(q)` הקנוני (מנוקד, עם אות התשובה), edge-tts (Avri/Hila). אל תפצל מחדש את התסריט — `[[PAUSE]]` כבר מפריד שאלה מתשובה.
- זה **נוסף** לצינור הקיים (`tools/quiz-app/update-question.js` דרך השרת), לא מחליף אותו. פרטים והחלטות ב-`docs/video-pipeline.md`.

## ויזואל רשיון 12 (שרשרת עדיפויות ב-scenes.js)
`scenes.js` מנתב ויזואל של l12 (מוגן `q.license === 12`, לא נוגע ב-11) לפי:
1. **שאלת "תמונה N"** → `mediaUrl` לתמונת חוברת הסימנים הרשמית ב-`media/signs/tmuna_NNN.png` (מוגש דרך raw.githubusercontent). `mediaUrl` דורס את הסצנה. 117 שאלות מחוברות. התמונות 1-126 = אורות לילה (1-74), סימני יום (75-90), דגלים (91-110), אותות קוליים (111-126). חולצו מ-`חוברת סימנים.pdf` (G:\My Drive\Sailor\zoom) עם PyMuPDF.
2. **שאלת שושנה/כיוון** → `generateCompassRoseScene`. 3. **זכות מעבר תיאורית** → `getScene` (מצב שיט). 4. **אחרת** → כרטיס נקי `neutralSceneL12()` (גלגל הצלה + עוגן).
- `data/l12.json`: `topic` סווג לטקסונומיית l11 (מותאם לסירת מנוע, נושא כללי = "סירת מנוע - כללי") ו-`mediaUrl`/`mediaType` הוצמדו — הכל in-place, byte-identical ל-`dumpCanonical`.
- ⚠️ תשובות l12 עדיין `answer_confirmed:false` — אמת מול המפתח הרשמי לפני רינדור לפרסום. אין `explanation` (0/315).

## API
- קריאות ה-LLM ב-`src/generate.js` ו-`src/scraper.js` משתמשות ב-Haiku (`claude-haiku-4-5-20251001`). השאלות הן JSON פשוט — אין צורך ב-Opus.

## טרמינולוגיה עברית לתוכן פונה-משתמש
- רשיון 11 = **"רשיון אופנוע ים"** (לעולם לא "רשיון 11") בכל טקסט שהמשתמש רואה.
- הבחינה = **"מבחן תאוריה"** (לא סתם "מבחן").
- אמת טרמינולוגיה מול המפתח הרשמי לפני יצירת copy כלשהו. (ניקוד ל-VO — ראה skill `hebrew-tts-niqqud`.)
- **כל נוסח פונה-משתמש** (טקסט מערכי שיעור, קריינות/VO, copy שיווקי) — הפעל את skill `no-ai-slop` על הטקסט לפני סיום, לזיהוי/ניקוי דפוסי AI (ניגודים בינאריים, throat-clearing, colon-reveals, importance puffery, סיומות סיכום, em dashes). הכללים המבניים חלים גם על עברית; רשימת המילים האסורה היא אנגלית. משלים את ה-anti-slop הגלובלי, לא מחליף.

## פתיחת סשן — אמת לפני שאתה פועל
- אם יש מסמך handover קודם, **קרא אותו במלואו** (לא רק את האינדקס), ואז אמת כל טענה על עבודה פתוחה מול `git log` והקבצים בפועל. סיכומים ישנים כבר טענו שדגלים 104/106 צריכים תיקון כשהם כבר commited.
- לפני עבודה על תוכן: הרץ `git status` ו-`git log origin/main..HEAD`. checkout מקומי תקוע כבר נראה כמו "שאלות נעלמו" ו-"cache בדפדפן". בדוק git לפני שאתה מאשים cache.
- אל תתחיל עבודה על סמך קונטקסט מסוכם עד שאישרתי שהוא עדיין נכון.

## סגנון עבודה
- **שאל לפני עבודה יקרה** (batch render, multi-file rewrite, גיאומטריית מפה מדויקת). תן תוכנית ב-3 בולטים + הערכת מספר קריאות כלים, וחכה ל"go".
- **שאלה-שאלה כברירת מחדל.** אל תעבור ל-batch אלא אם ביקשתי במפורש.
- **אל תבצע over-engineering.** "מספיק טוב וויזואלית נכון" מנצח דיוק גיאודזי באיורי חידון. אם משהו נראה כמו יותר מ-10 קריאות כלים — הצע גרסה זולה קודם.

## שכבת נתונים — אינטגריטי
- אל תרנדר/תיצור נגזרות משאלה שלא אומתה מול מפתח התשובות הרשמי. אימות תוכן קודם, רינדור אחר כך.
- אל תרג'נרט את `data/l11.json`/`questions.json` בסקריפטים חד-פעמיים. ערוך in-place ושמר את כל השדות הקיימים, במיוחד `mediaUrl`.
- לפני רג'נרוט bulk: הרץ diff ברמת שדה מול HEAD ודווח אילו מפתחות נוספו/נמחקו.
- EOL נאכף עכשיו ע"י `.gitattributes` — אין יותר ריקוד ידני. מקור (`*.js/*.html/*.css/*.md/*.py/*.json`) = **LF**, `data/*.json` = **-text** (בייטים גולמיים, CRLF כמו שה-editor כותב דרך ה-API), `questions.json` = LF. כלי Edit/Write כותבים CRLF בעץ-העבודה אבל git מנרמל ל-LF ב-commit, אז אין churn. **אל תוסיף חוק `text` על `data/*.json`** — זה יאחסן blob ב-LF ויתנגש עם הכתיבה הגולמית של github-api.js. `core.autocrlf=false`. אם משהו נראה מוזר, אמת עם `git diff --ignore-cr-at-eol --stat`.

## Shell על Windows
- **אל תשתמש ב-heredocs של bash (`<<EOF`)** — הם קרסו סשנים חזור על המכונה הזו. כתוב סקריפט לקובץ `.py`/`.js` עם Write והרץ אותו.
- Python לפרויקט מוצמד ל-3.12 דרך `uv`; 3.14 שובר את CrewAI.
- `PYTHONIOENCODING=utf-8` לפני כל סקריפט שמדפיס עברית.
- `git push` דורש PAT; ה-prompt האינטראקטיבי של Git Credential Manager תולה את הסשן. ודא שה-credential helper מוגדר לפני push.
