---
name: hebrew-tts-niqqud
description: ניקוד והגייה נכונה לקריינות עברית ב-Higgsfield עם הקול Remy. השתמש בכל פעם שאלכס מבקש להפיק אודיו/קריינות/voiceover בעברית, מכין תסריט לסרטון SkipperQuiz, או שולח טקסט עברי ל-generate_audio. גם אם הבקשה נשמעת כללית כמו "תפיק קריינות" או "תהפוך ל-voice" — טען skill זה קודם, החל את מילון הניקוד על הטקסט, ועבוד משפט-משפט מול המשתמש לאישור לפני הרכבה.
---

# Hebrew TTS Niqqud — קריינות עברית מדויקת ל-Higgsfield/Remy

## מטרה
מנוע ה-TTS (text2speech_v2, variant=elevenlabs, voice Remy) קורא עברית לא־מנוקדת וטועה במילים מסוימות. skill זה מחזיק מילון תיקונים מאושר ומגדיר את שיטת העבודה כדי שכל קריינות תצא נכון.

## הגדרות ההפקה הקבועות
- model: `text2speech_v2`
- variant: `elevenlabs`
- voice_type: `preset`
- voice_id: `b9c5c5db-4eb7-468a-a0b7-d06423af0335` (Remy)

## שיטת העבודה (חובה)
1. **החל את המילון על הטקסט** לפני כל שליחה. קרא `references/niqqud-dictionary.md` והחל את התיקונים, או הרץ `scripts/apply_niqqud.py`.
2. **עבוד משפט-משפט.** אל תפיק את כל הקריינות בבת אחת בפעם הראשונה. הפק משפט/מקטע, המשתמש שומע ומאשר, ורק אז ממשיכים. הסיבה: אין למשתמש קול עברי במערכת שלו — הבדיקה חייבת לעבור דרך הפקת Remy אמיתית.
3. **תיקון בזמן אמת.** אם מילה נשמעת רע — נסה ניקוד, ואם לא עוזר, עקוף בניסוח (ראה טבלת עקיפות במילון).
4. **הרכבה בסוף.** אחרי שכל המשפטים אושרו, הרכב עם `explainer_video` (per-block audio) או FFmpeg אצל המשתמש.
5. **עדכן את המילון.** בכל פעם שנפצחת מילה חדשה — הוסף אותה גם ל-`references/niqqud-dictionary.md` וגם ל-`REPLACEMENTS` ב-`scripts/apply_niqqud.py`, כדי שתיושם אוטומטית בעתיד.

## טעינת/הרצת המילון
```bash
python scripts/apply_niqqud.py "טקסט הקריינות כאן"
```
המילון המלא, כולל הערות וטבלת עקיפות, נמצא ב-`references/niqqud-dictionary.md`. קרא אותו כשצריך להבין למה מילה מנוקדת בצורה מסוימת או להוסיף מילה.

## פתרון תקלות
- **קובץ אודיו לא נוצר / ג'ובים נכשלים ברצף:** בדוק עם `show_generations(type=audio)` אם ההפקה החדשה נכנסה לרשימה. אם לא — תקלה זמנית בצד Higgsfield, לא בטקסט. המתן והפק שוב.
- **הווידג'ט/נגן לא נטען אצל המשתמש:** רענון קשה (Ctrl+Shift+R) או ניקוי קאש. חלופה: שלוף לינק ישיר מ-`show_generations` להורדה מחוץ לצ'אט.

## כללים כלליים (מתוך המילון)
- לנקד רק מילים מהרשימה, לא את כל הטקסט.
- מספרים במילים ("שבעים מטר").
- לועזית/ראשי תיבות בכתיב פונטי עברי ("צ'רט פלוטר").
